-- =====================================================================
-- 20260705_po_line_sent.sql
-- 採購專區：進度改三態（未發出 → 製作中 → 已出貨）
-- 建立日期：2026-07-05
--
-- OPEN 不代表已發給廠商，故新增 sent_at：
--   sent_at 為 null       → 未發出
--   sent_at 有值、未出貨   → 製作中（已發給廠商）
--   shipped_at 有值        → 已出貨
-- 純附加欄位，既有列 sent_at 預設 null（＝未發出），不影響現有資料。
-- =====================================================================

alter table public.po_line_tracking
  add column if not exists sent_at timestamptz;

-- =====================================================================
-- 完
-- =====================================================================
