# 新增：異常回報單（業務美編）

發布日期：2026-08-26
範圍：首頁「異常單回報」彈窗＋新頁面。既有的建立異常單/異常單處理不動。

---

## 公告欄白話版

> 🆕 **異常單彈窗多了一張卡**
> 首頁點「異常單回報」，最左邊多了「異常回報單（業務美編專用）」。
> 內容目前與一般異常回報單相同，後續會依業務美編的需求調整。

## 內容

- 首頁異常單彈窗新增左側卡片「異常回報單／業務美編專用」，點了進 `/qa/report-sales-design`；彈窗由 max-w-md 放寬為 max-w-2xl 讓三張卡並排。
- 新頁面為既有異常回報單（`app/admin/production/anomaly/report/page.tsx`）的**完整複本**，僅改頁面標題為「異常回報單（業務美編）」。**兩頁刻意不共用**：這頁之後要依業務美編需求各自演進，改它不會動到原本的表單。
- 權限沿用 `/qa` 前綴的既有守衛（`qa_report`），送單同樣寫入 `schedule_anomaly_reports`。

## 驗證

- `next build --webpack` 全綠（128/128 頁，`/qa/report-sales-design` 已註冊）。
- 本機實開：新頁標題正確、異常分類/部門選單從資料庫載入成功；原 `/qa/report` 完全不變。
- `tsc --noEmit` ✅；eslint 僅剩原檔既有的 `<img>` 警告（隨複本帶入，非新增錯誤）。

## 變更檔案

新增：`app/qa/report-sales-design/page.tsx`（複本）
修改：`app/page.tsx`（彈窗加卡片＋放寬）
