# 訂單修改紀錄：發單日獨立成欄＋欄距微調 (2026-09-17)

## 本次更新重點

> 接續今天稍早的發單前後判定修正，依 Snow 的版面回饋做三個調整：
> 1. **「發單日」獨立成一欄**，放在「發單狀態」前面——出單表的實際發單日直接看得到，
>    「發單前改」綠色標籤也移到這一欄（它是時間比較的結果，跟旁邊的現況狀態是兩回事）。
>    出單表查無的單標「查無」（琥珀色）、未發單顯示「—」。
> 2. 「動作」欄往左移一點（縮左間距）。
> 3. 「應通知」欄往右移一點（加左間距）。

## 技術

- 只動 `app/admin/argoerp/erp-sync/SoEditLogCard.tsx`：新增發單日 td／th、
  發單狀態欄移除原本塞在裡面的發單日小字與綠標、`colSpan` 9→10、
  動作欄 `pl-1 pr-2`、應通知欄 `pl-6 pr-3`。API 回傳不變。

## 驗證

- `npx tsc --noEmit` 0 errors。
- 本機 dev（webpack 模式）+ 攔截 API 餵四種情境（發單前改／發單後改／查無／未發單）
  實際渲染確認：10 欄對齊、發單日與綠標顯示正確、發單狀態欄乾淨。
