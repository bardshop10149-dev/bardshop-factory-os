# ARGO 請購單自動傳簽 — 從零開始設定

2026-09-24

---

## 先講結論：你的電腦**什麼都不用裝**

2026-09-24 實際掃過這台機器，需要的東西全都已經在了：

| 項目 | 狀態 | 位置 |
| --- | --- | --- |
| PowerShell | ✅ 5.1 已內建 | Windows 11 專業版 |
| Java | ✅ JDK 17 已安裝 | `C:\Program Files\Java\jdk-17` |
| Java Access Bridge 程式 | ✅ 已存在 | `C:\Program Files\Java\jdk-17\bin\jabswitch.exe` |
| Access Bridge DLL（64 位元） | ✅ 已存在 | `C:\Windows\System32\WindowsAccessBridge-64.dll` |
| ARGO 主程式 | ✅ 已安裝 | `C:\Users\USER\AppData\Local\ArgoERP\ArgoERP.exe` |

**缺的不是軟體，是設定**：Java Access Bridge 還沒啟用（步驟 2）。

---

## 動手之前：三個關於 ARGO 的事實

這三件事決定了整套自動化長什麼樣子，先看懂會省下很多摸索。

### ① ARGO 的畫面是 Java（Oracle Forms），不是一般 Windows 程式

`ArgoERP.exe` 只有 1MB，它是個**啟動器**。真正的畫面來自同目錄下的
`frmsal.jar`（Oracle Forms Standalone Application Launcher），由 Java 跑起來。

**所以**：自動化要找的視窗屬於 **java/javaw 程序**，不是 `ArgoERP.exe`。
實測 `ArgoERP.exe` 的視窗標題是空的，拿它當目標一定抓不到。

### ② 閒置 15 分鐘自動斷線

排程在 17:40 跑的時候，ARGO 一定早就斷了。所以腳本必須**自己完整登入**，
不能假設視窗開著。

副作用：斷線後 `ArgoERP.exe` 不會自己結束。掃描當下就看到三個殘留程序
（9/16、9/18、9/21 啟動的，都沒有視窗）。腳本啟動前會先清掉這些。

### ③ 傳簽沒有 API，只能按畫面

2026-09-24 向 ARGO 廠商確認，三個答案都是不行：

1. `IFAF105` 不支援更新既有請購單的 `HOLD_STATUS`（UNSIGNED → SIGNING）
2. 沒有專門的「傳簽」介面
3. 傳簽文號／傳簽類別不能由 API 帶入

我們自己唯讀查 `PJ_APPLYPROJECT` 也印證了——傳簽實際改了三個欄位：

| 欄位 | 未傳簽 | 已傳簽 |
| --- | --- | --- |
| `HOLD_STATUS` | `UNSIGNED` | `SIGNING` |
| `SIGN_FLAG` | `null` | `Y` |
| `PFM_DOCUMENT_NO` | `null` | `PBM260924019` |

關鍵在 `PFM_DOCUMENT_NO`：傳簽真正做的事是**在簽核模組另外產生一張 PBM 文件**。
就算能改 `HOLD_STATUS`，沒有那張文件，簽核流程還是不會跑。

> 註：查 `PFM_*` 表會回「WebService 未授權」，但實測打一個**根本不存在的表名也回同一句**，
> 所以不能拿它當「開了權限就能做」的依據。

---

## 步驟 1：設定環境變數（3 分鐘）

開 PowerShell（一般權限即可），貼上執行：

```powershell
[Environment]::SetEnvironmentVariable('ARGO_SIGN_BASE_URL', 'https://bardshop-eip.vercel.app', 'User')
[Environment]::SetEnvironmentVariable('ARGO_SIGN_SECRET',   '<這裡填 WEBHOOK_SECRET>', 'User')
[Environment]::SetEnvironmentVariable('ARGO_SIGN_EXE',      'C:\Users\USER\AppData\Local\ArgoERP\ArgoERP.exe', 'User')
```

`ARGO_SIGN_SECRET` 是**這支腳本專屬**的鑰匙，要自己產一把、在 Vercel 和本機各設一次。

為什麼不共用 `WEBHOOK_SECRET`：
- **最小權限**：這把只開得了「讀待傳簽清單／查單張狀態」，觸發不了任何排程端點。
  腳本跑在辦公室的個人電腦上，萬一外流，損害範圍差很多。
- **實務上也拿不到**：Vercel 把 `WEBHOOK_SECRET` 設成 Sensitive，存進去就讀不出來，
  「Copy to Clipboard」是灰的，只能覆寫不能看。去改它還會連帶影響其他在用的排程。

產鑰匙（在 PowerShell 打）：

```powershell
[Convert]::ToBase64String((1..24 | ForEach-Object { Get-Random -Max 256 })) -replace '\+','-' -replace '/','_' -replace '='
```

把產出的字串**同時**設到兩個地方：
1. Vercel → 專案 → Settings → Environment Variables → Add New，
   Key 填 `ARGO_SIGN_SECRET`，Value 貼字串，Environment 勾 **Production**，存檔後**重新部署**
2. 本機（下面的第三行指令）

設完**關掉這個 PowerShell 視窗再重開**，新視窗才讀得到。驗證：

```powershell
$env:ARGO_SIGN_BASE_URL   # 應該印出網址
$env:ARGO_SIGN_EXE        # 應該印出 ArgoERP.exe 路徑
```

**關於 ARGO 帳密**：不用設。腳本靠 ARGO 登入畫面本來就勾著的「Remember me」，
直接按 Login。密碼不進腳本、不進環境變數、不進 git。只有在 Remember me 不可靠時
才設 `ARGO_SIGN_USER` / `ARGO_SIGN_PASSWORD`——那等於把 ERP 密碼放進這台電腦的
使用者環境變數，要清楚這個代價。

---

## 步驟 2：啟用 Java Access Bridge（2 分鐘，要重開機）

沒開的話，自動化工具看不到 Java 視窗裡的「按鈕」「輸入框」，只能靠螢幕座標點擊——
座標會因視窗位置、解析度、縮放比例而跑掉，換台電腦或拖動視窗就壞。

```powershell
& 'C:\Program Files\Java\jdk-17\bin\jabswitch.exe' -enable
```

看到 `The Java Access Bridge has been enabled.` 就成功了。**然後重開機**。

重開機後驗證（檔案存在就是開了）：

```powershell
Test-Path "$env:USERPROFILE\.accessibility.properties"
```

> ARGO 可能跑 32 位元的 Java（`javaUrl.txt` 裡寫的是 `jre-8u151-windows-i586.exe`，
> i586 = 32 位元）。這台機器只有 64 位元的 `WindowsAccessBridge-64.dll`，沒有 32 位元版。
> 步驟 3 會確認 ARGO 實際跑的是哪一個——如果是 32 位元，得另外裝 32 位元 JRE
> 或改用座標點擊。先做步驟 3 再說。

---

## 步驟 3：抓 ARGO 的視窗標題（校準第一步）

腳本要靠視窗標題判斷「現在走到哪一步」。標題得實際抓，不能猜。

**手動**把 ARGO 開到第一個畫面（啟動器，就是有 BARDSHOP / TEST 兩顆按鈕的小視窗），
然後另開一個 PowerShell：

```powershell
cd C:\Users\USER\factory-scheduler\scripts\argo-sign
.\capture-argo-ui.ps1 -Watch
```

它每 2 秒掃一次，把屬於 ARGO/Java 的視窗用綠色 ★ 標出來。**讓它一直跑著**，
然後手動把 ARGO 一步步操作過去：

| 操作 | 要記下的東西 |
| --- | --- |
| 啟動器出現 | 視窗標題、程序名稱 |
| 點 BARDSHOP → 登入畫面 | 標題（截圖看到的是 `APPF00061 v4.67`） |
| 按 Login → 主選單 | 標題 |
| 點「原物料請購作業」 | 標題（截圖看到的是 `原物料請購作業–PJAF084 v4.23 (BARDSHOP)`） |

順便確認：**★ 那幾行的程序名稱是 `java` 還是 `javaw`**，路徑是不是
`C:\Program Files\Java\jdk-17`。若指向別的 32 位元 Java，回頭看步驟 2 的註記。

把這四個標題貼給我，或自己填進 `argo-sign.ps1` 裡 `Wait-Window` 的比對字串。

---

## 步驟 4：先跑 DryRun，確認跟系統接得通

這一步**完全不碰 ARGO**，可以安心跑：

```powershell
cd C:\Users\USER\factory-scheduler\scripts\argo-sign
.\argo-sign.ps1 -DryRun
```

應該會列出今天開的單號與各自狀態，例如：

```
日期 2026-09-24：今天開出 2 張，其中待傳簽 1 張
  MPO2026092401  pr  UNSIGNED
  POC2026092401  po  OPEN
```

跑不出來先看：環境變數有沒有設對、`WEBHOOK_SECRET` 對不對、網址連不連得到。

---

## 步驟 5：校準按鍵動作

`argo-sign.ps1` 裡有 **8 個校準點**，標著 `▼ 校準點 N`：

| 位置 | 校準點 | 做什麼 |
| --- | --- | --- |
| `Start-Argo` | 1 | 啟動器上選公司別（BARDSHOP） |
| | 2 | 登入畫面填帳號（用 Remember me 就跳過） |
| | 3 | 送出登入 |
| | 4 | 主選單開「原物料請購作業」（右側「我的最愛」第一項） |
| `Sign-OneDoc` | 5 | 進入查詢模式 |
| | 6 | 輸入請購單號 |
| | 7 | 執行查詢 |
| | 8 | 按「傳簽」 |

**做法**：手動做一次完整流程，把每一步用到的**鍵盤**操作記下來，替換進對應校準點。

ARGO 是 Oracle Forms，大多數操作都有鍵盤對應——查詢通常是 `F7` 進查詢模式、`F8` 執行。
「傳簽」若沒有快捷鍵，就得靠 Access Bridge 依按鈕名稱點擊（這就是步驟 2 的用意）。

校準時用 `-KeepOpen`，跑完 ARGO 不會關掉，方便看卡在哪一步：

```powershell
.\argo-sign.ps1 -KeepOpen
```

---

## 步驟 6：設排程

Windows 工作排程器，建議 **17:40**——兩輪自動轉單（17:10／17:35）都跑完之後、
伺服器端 17:50 檢查之前。這樣腳本失敗時，17:50 那道防線還來得及發現。

以**系統管理員**開 PowerShell：

```powershell
$act = New-ScheduledTaskAction -Execute 'powershell.exe' `
  -Argument '-NoProfile -ExecutionPolicy Bypass -File "C:\Users\USER\factory-scheduler\scripts\argo-sign\argo-sign.ps1"'
$trg = New-ScheduledTaskTrigger -Daily -At 17:40
Register-ScheduledTask -TaskName 'ARGO請購單自動傳簽' -Action $act -Trigger $trg -RunLevel Highest
```

**必看**：工作排程器要設成「**只在使用者登入時執行**」。UI 自動化需要互動式桌面，
勾「不論使用者是否登入」會讓腳本在看不見的工作階段裡跑，什麼都按不到，而且失敗得很安靜。

---

## 這套為什麼這樣設計：每按一張就驗一張

桌面自動化的失敗幾乎都是**靜默**的——視窗沒開、欄位沒對焦、按鈕位置跑掉，
腳本照樣跑完並回報成功。**沒有驗證的自動化比沒有自動化更危險**，因為不會有人發現。

```
系統告訴腳本「今天該按哪幾張」 → 按一張 → 立刻回查 ARGO 狀態
                                   ↑              │
                                   └── 有變才繼續 ─┘
                                       沒變就停 + 記 log + 回非 0
```

而且伺服器端還有一道**獨立**防線：每天 17:50 的 `pr-sign-check` 會自己查一次，
還有沒傳簽的就發 LINE。**就算本機腳本整個沒跑、電腦關機，你也會知道。**

伺服器端端點：

| 端點 | 用途 |
| --- | --- |
| `GET /api/argoerp/pending-sign` | 今天待傳簽的單號 |
| `GET /api/argoerp/pending-sign?verify=單號` | 查單張現在的狀態 |
| `GET /api/argoerp/pending-sign?date=YYYY-MM-DD` | 指定日期補查 |
| `/api/cron/pr-sign-check`（每天 17:50） | 獨立防線，沒傳簽就發 LINE |

---

## 已知限制

- 腳本一遇到驗證失敗就**停**，不繼續處理後面的單。這是故意的：同一個 UI 問題通常會
  讓後面每一張都失敗，繼續按只是把錯誤放大。
- 單量大到單次處理超過 15 分鐘而中途斷線時，逐張驗證會抓到（狀態沒變就停），
  不會默默跳過後面的單。
- ARGO 改版或畫面調整後，視窗標題與按鍵可能要重新校準。log 會直接告訴你卡在哪一張、
  當時狀態是什麼。
- log 在 `scripts\argo-sign\logs\argo-sign-YYYYMMDD.log`。

---

## 檔案一覽

| 檔案 | 用途 |
| --- | --- |
| `scripts/argo-sign/argo-sign.ps1` | 主腳本（排程跑這支） |
| `scripts/argo-sign/capture-argo-ui.ps1` | 校準用的視窗偵測工具（唯讀，不點任何東西） |
| `scripts/argo-sign/logs/` | 執行紀錄（已加入 .gitignore） |
| `app/api/argoerp/pending-sign/route.ts` | 待傳簽清單／單張狀態查詢 |
| `app/api/cron/pr-sign-check/route.ts` | 17:50 的獨立防線 |
